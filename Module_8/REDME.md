Instructions: Write the JavaScript code and show the output in the console.

Question 1: Array
Create an array:

const fruits = ["Apple", "Banana", "Mango"];

Add "Orange" using push().

Remove the last element using pop().

Question 2: Array Methods
Create an array:

const numbers = [10, 20, 30, 40, 50];


Use map() to multiply each number by 2.

Use filter() to get numbers greater than 25.

Question 3: Object
Create an object:

const student = {
name: "Rahim",
age: 22
};

Print all keys using Object.keys().

Check if the object has the property "age".

Question 4: Spread Operator
Merge the following arrays using the spread operator:

const arr1 = [1, 2, 3];
const arr2 = [4, 5, 6];

Question 5: Rest Parameter
Create a function that takes any number of arguments and returns their sum.

Example:
sum(10, 20, 30)

Output:
60

Question 6: Hoisting
What will be the output of the following code?

console.log(a);
var a = 10;